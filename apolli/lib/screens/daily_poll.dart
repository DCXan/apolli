import 'package:flutter/material.dart';

class DailyPollScreen extends StatelessWidget {
  const DailyPollScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Daily Poll Screen',
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
